import React from 'react';

import { Panel, PanelHeader, } from '@vkontakte/vkui';

const Home = ({ id }) => (
	<Panel id={id}>
		<PanelHeader>Example</PanelHeader>

	</Panel>
);


export default Home;
